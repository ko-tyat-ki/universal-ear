// -------------------------------------------------- //
// This file is autogenerated by pioasm; do not edit! //
// -------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

// ------------------- //
// ws2812_parallel_16b //
// ------------------- //

#define ws2812_parallel_16b_wrap_target 0
#define ws2812_parallel_16b_wrap 1

#define ws2812_parallel_16b_T1 2
#define ws2812_parallel_16b_T2 5
#define ws2812_parallel_16b_T3 3

static const uint16_t ws2812_parallel_16b_program_instructions[] = {
            //     .wrap_target
    0x6030, //  0: out    x, 16                      
    0xa101, //  1: mov    pins, x                [1] 
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program ws2812_parallel_16b_program = {
    .instructions = ws2812_parallel_16b_program_instructions,
    .length = 2,
    .origin = -1,
};

static inline pio_sm_config ws2812_parallel_16b_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + ws2812_parallel_16b_wrap_target, offset + ws2812_parallel_16b_wrap);
    return c;
}

#include "hardware/clocks.h"
static inline void ws2812_parallel_16b_program_init(PIO pio, uint sm, uint offset, uint pin_base, uint pin_count, float freq) {
    for(uint i = pin_base; i < pin_base + pin_count; i++) {
        pio_gpio_init(pio, i);
        gpio_set_function(i, pio == pio0 ? GPIO_FUNC_PIO0 : GPIO_FUNC_PIO1);
    }
    pio_sm_set_consecutive_pindirs(pio, sm, pin_base, pin_count, /*is_out=*/true);
    pio_sm_config c = ws2812_parallel_16b_program_get_default_config(offset);
    sm_config_set_out_shift(&c, /*shift_right=*/true, /*autopull=*/true, /*pull_threshold=*/32);
    sm_config_set_out_pins(&c, pin_base, pin_count);
    sm_config_set_set_pins(&c, pin_base, pin_count);
    sm_config_set_fifo_join(&c, PIO_FIFO_JOIN_TX);
    int cycles_per_bit = 1;
    float div = clock_get_hz(clk_sys) / (freq * cycles_per_bit);
    sm_config_set_clkdiv(&c, div);
    pio_sm_init(pio, sm, offset, &c);
    pio_sm_set_enabled(pio, sm, true);
}

#endif

